# Deploy Pipeline & Working Conventions

_Ingested 2026-08-22 from the Standing project facts in [CLAUDE.md](../../CLAUDE.md). Pipeline claims verified against [netlify.toml](../../../netlify.toml) and the working tree; the workflow conventions below are process facts sourced only from CLAUDE.md — see [Citation gap](#citation-gap)._

## Netlify

- Publish directory is the repo root: `publish = "."` ([netlify.toml:2](../../../netlify.toml)). Everything not explicitly blocked is publicly served.
- Functions directory: `netlify/functions` ([netlify.toml:19](../../../netlify.toml)).
- `vault/` is blocked from public access by a forced 404 redirect (`from = "/vault/*"`, `status = 404`, `force = true`) — added in commit `afae167`, 2026-08-22. `force = true` is what makes it win over the real files on disk, which matters given `publish = "."`. There is no `/404` page in the repo, so Netlify serves its default 404 body. **Verified live 2026-08-23:** `/vault/`, `/vault/CLAUDE.md`, and `/vault/wiki/index.md` all return 404 ([decision record](../../raw/decisions/2026-08-22-vault-setup-and-tts-endpoint.md#2-netlify-exposure--blocked)).
- **Anything not explicitly blocked is public the moment it is committed.** No admin dashboard exists in the repo or on the site — `git ls-files` matches nothing for admin/dashboard, and `/admin`, `/admin.html`, `/admin/index.html` all 404 (checked 2026-08-23). If one is ever built, the forced-404 rule has to land *before* the commit, the same treatment `vault/` got.
- Headers set MIME types for `still-mobile` audio and cache `/api/sing-the-hours` ([netlify.toml](../../../netlify.toml)).

### ⚠ Standing risk — `publish = "."` makes every new root directory public

**This is a footgun, not a one-off.** Because the publish directory is the repo
root, *any* directory added at the top level is served on the public site from
the moment it is deployed, with no warning and no opt-in. The default is
exposure; blocking is the exception someone has to remember.

It has already caught us once. `corpus/` — 23 MB of generated Office JSON —
was committed in `1de6c29` on 2026-08-24 and was publicly reachable at
`stillprayer.app/corpus/traditional/…` until `c210e4b` blocked it the same day
([decision record](../../raw/decisions/2026-08-24-office-corpus-json-shape.md)).
Nothing sensitive was in it, but the mechanism is indifferent to what the
directory contains.

**Four directories are currently blocked**, all by the same forced-404 pattern:

| Path | Blocked in | What it holds |
|---|---|---|
| `/vault/*` | `afae167` (2026-08-22) | this knowledge vault |
| `/supabase/*` | `afae167` (2026-08-22) | database migrations |
| `/corpus/*` | `c210e4b` (2026-08-24) | generated Office corpus, ~23 MB |
| `/tools/*` | `c210e4b` (2026-08-24) | build-time generators |

`force = true` is what makes each win over the real files on disk.

**A block stops access, not upload.** With `publish = "."` every blocked file
is still pushed to the CDN on each deploy, so `corpus/` continues to cost
deploy time and bandwidth while returning 404. The redirect closes the exposure
and nothing else.

**The proper fix is a real publish directory** — build into a dedicated folder
and point `publish` at it, so the default flips from "everything is public" to
"only what was built is public". That is a change to how every asset deploys on
a live app, so it is **deferred to a planned deploy window before Advent
(2026-11-29)** rather than made incidentally. Until then, the rule stands:

> Adding a directory at the repo root? Add its forced-404 redirect in the
> **same commit**, or it ships public.

## www/ is a manual build output

[`www/`](../../../www) is **not** generated by a build step — it is hand-synced, and Capacitor's `webDir` points at it ([capacitor.config.json](../../../capacitor.config.json)), so native builds ship whatever is in `www/` at copy time.

```
cp index.html www/index.html && cp bell-native.js www/bell-native.js && cp -r still-mobile/. www/still-mobile/ && cp -r assets/. www/assets/
```

Verified 2026-08-22: `www/` contains `index.html`, `bell-native.js`, `assets/`, `still-mobile/`, plus a `netlify.toml` not listed in the sync command.

> **Drift is the default failure mode here.** As of 2026-08-22 `www/index.html` (15,770 lines) differs from root `index.html` (15,773) — the root has uncommitted edits that have not been synced. A native build made right now would ship the older page. Re-run the sync before any iOS/Android build.

**Drift is now guarded, for `index.html` only.** It happened again on
2026-08-25: the buildOffice split landed in root `index.html` while
`www/index.html` still held all three of the old renderers, and every office
test passed anyway because they all read the root copy. `test-build-office.js`
now opens with a PART 0 byte-comparison of the two files and exits non-zero if
they differ, so that particular staleness can no longer pass 301 green checks
(`2d5c074` synced it, the guard landed after). **The other three sync targets —
`bell-native.js`, `still-mobile/`, `assets/` — have no such guard.**

### ⚠ Standing risk — `www/netlify.toml` is a second, unguarded deploy config

`www/` carries its own [netlify.toml](../../../www/netlify.toml), noted above as
"not listed in the sync command". It also declares `publish = "."`, and it is a
**stale fork** of the root config: it has none of the four forced-404 blocks the
root config carries.

| Blocked path | root `netlify.toml` | `www/netlify.toml` |
|---|---|---|
| `/vault/*` | yes (`afae167`) | **no** |
| `/supabase/*` | yes (`afae167`) | **no** |
| `/corpus/*` | yes (`c210e4b`) | **no** |
| `/tools/*` | yes (`c210e4b`) | **no** |

The exposure is conditional, not live: it depends on which directory the
Netlify site's *base directory* setting points at, and that setting is not in
the repo — it lives in the Netlify UI, so it cannot be verified from here.
Today's deploys evidently use the root config, since `/vault/*` was verified
returning 404 live on 2026-08-23. **If the base directory is ever pointed at
`www/`, or a second site is created from it, the vault ships public** — the
same mechanism as the `corpus/` incident, with nothing to catch it.

This is the `publish = "."` footgun with a second copy of the gun. It is not
fixed here because it is a live-deploy decision, not a code change: **deferred
to the same planned pre-Advent deploy window (2026-11-29)** as the real-publish-
directory fix above, which would retire both configs together. Recorded
2026-08-25.

Interim rule, if the window slips:

> Do not point any Netlify base directory at `www/` until its config carries
> the four forced-404 blocks, or the real publish directory lands.

## Build environments

- Windows + VS Code is the primary development environment.
- iOS builds only on MacInCloud, from `~/still-app`.

## Git conventions

- Always `git add` specific files. **Never `git add .`** — the working tree routinely carries unrelated in-progress edits (e.g. `index.html`, `diff.txt`, `node_modules/`).
- Every code change should be accompanied by its push command.

## Citation gap

The build-environment and git conventions above have no source in `raw/` — they exist only as standing facts in the vault charter, [CLAUDE.md](../../CLAUDE.md). Per the ingestion protocol every wiki claim should cite a raw source. If these should be independently citable, they want a `raw/decisions/` record; flagging rather than back-filling one, since inventing a source document would violate the no-fabrication rule.
